import { regList } from "./regList"
import { toRule } from "./toRule"

/**
 * @name 正则 类方法
 * @example
 * regList         // 常见格式规则
 * toRule          // 判断是否为指定格式
 */
export const CSRegexp = {
  regList,
  toRule
}
