import { toFixed } from './toFixed';
import { toJson } from './toJson';
import { toString } from './toString';


/**
 * @name 格式类方法
 * @example	
 * toFixed      //保留小数点（非四舍五入）
 * toJson       //变为json格式
 * toString     //转为字符串
 */
export default {
    toFixed,
    toJson,
    toString
};
